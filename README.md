# Pair Hash

Narrow integration of Pair with Hash. Pair owns the two-value container, Pair
Equation supplies its conditional equality, and this molecule owns
element-wise hashing plus conditional native `Hashable` and domain-typed
`Hash.Protocol` conformances, including noncopyable payloads.
